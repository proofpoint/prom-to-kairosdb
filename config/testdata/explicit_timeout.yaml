kairosdb-url: "abc.com"
timeout: 10s
metric_relabel_configs:
   - source_labels: [ __name__ ]
     regex: 'my_too_large_metric'
     action: drop
   - source_labels: [ __name__ ]
     regex: 'my_too_large_metric'
     action: drop
